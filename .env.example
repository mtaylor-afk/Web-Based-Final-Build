# Database — SQLite for local dev
DATABASE_URL="file:./dev.db"

# For PostgreSQL (production), replace with:
# DATABASE_URL="postgresql://user:password@localhost:5432/wv_construction?schema=public"

# AI Provider — OpenAI
OPENAI_API_KEY="sk-..."

# AI Image Generation (DALL-E 3 used for visuals)
# Uses same OPENAI_API_KEY above

# App
NEXTAUTH_SECRET="your-secret-here"
NEXTAUTH_URL="http://localhost:3000"

# File upload storage path (relative to project root)
UPLOAD_DIR="./public/uploads"
